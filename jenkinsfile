pipeline {
    agent any

    stages {
        stage('Checkout SCM') {
            steps {
                checkout scm
            }
        }

        stage('Hello') {
            steps {
                echo 'Jenkins CI/CD with GitHub is running'
            }
        }
    }
}
